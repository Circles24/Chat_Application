# Chat_Application
Simple chat application

first time users
`chmod +x *.sh`

Building the application
`./Build.sh`

Starting the server
`./Server.sh`

Starting a client
`./Client.sh`

for flexibility modify config files
